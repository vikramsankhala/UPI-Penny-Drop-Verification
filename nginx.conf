# Nginx config for gopalsankhala.in
# Place in /etc/nginx/sites-available/penny-drop and symlink to sites-enabled

server {
    listen 80;
    server_name gopalsankhala.in www.gopalsankhala.in;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    client_max_body_size 5M;
}

# Uncomment for HTTPS (after obtaining SSL cert via certbot)
# server {
#     listen 443 ssl http2;
#     server_name gopalsankhala.in www.gopalsankhala.in;
#     ssl_certificate /etc/letsencrypt/live/gopalsankhala.in/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/gopalsankhala.in/privkey.pem;
#     location / {
#         proxy_pass http://127.0.0.1:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }
# }
